# q16 — Control plane down due to a static pod manifest error

| Item | Value |
|---|---|
| Exam | mock_exam_a |
| Domain | Troubleshooting (30%) |
| Points | 5 |
| Context | ssh to the control plane node (`kubectl` does not work at all) |
| Target time | 8 min |
| Result | ☐ correct ☐ partial ☐ wrong |

## Task

Every `kubectl` command fails with `The connection to the server ... was refused`. The API
server is not starting.

1. Log in to the control plane node and find the cause.
2. Recover the API server so that `kubectl get nodes` works again.
3. Record the cause in one line.

## My attempt

```bash

```

---

Solution → **[solution.md](solution.md)**
